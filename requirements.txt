# TASMAC Proximity Scanner v4 — Dependencies
# Install: pip install -r requirements.txt

aiohttp==3.9.5        # async HTTP client (replaces requests)
aiofiles==23.2.1      # async file I/O
tqdm==4.66.4          # live progress bars
uvloop==0.19.0        # fast event loop for Apple Silicon (M1/M2/M3)